# Backend API manzili (build vaqtida JS ichiga yoziladi)
# Dev rejimda bo'sh qoldiring — vite proxy /api ni ishlatadi.
# Production build uchun to'liq manzil:
VITE_API_URL=https://api.example.com/api
